# SandyHeroes 프로젝트 리팩토링 결과 보고서

## 1. 개요
본 보고서는 SandyHeroes 프로젝트(Client 및 Server)의 아키텍처 및 메모리 관리 구조를 개선하기 위해 진행된 대대적인 리팩토링의 결과를 요약합니다. 이번 리팩토링은 프로젝트의 코드 컨벤션에 맞춰 모던 C++의 스마트 포인터(`shared_ptr`, `weak_ptr`)를 전면 도입하여 메모리 누수 및 댕글링 포인터(Dangling Pointer) 문제를 근본적으로 해결하는 데 목적이 있습니다.

## 2. 주요 개선 사항

### 2.1. 스마트 포인터 기반의 소유권 및 생명주기 관리
- **`shared_ptr` / `weak_ptr` 전면 적용**: `Object`와 `Component` 계층을 비롯한 게임 내 핵심 엔티티의 생명주기 관리를 기존 로우 포인터(Raw Pointer) 기반에서 스마트 포인터 기반으로 완전히 전환하였습니다.
- **순환 참조(Circular Reference) 방지**: 부모-자식 관계나 컴포넌트-소유자(`owner_`) 관계, 타겟 추적 등 역참조가 일어나는 모든 지점에 `weak_ptr`를 일관되게 도입하여 순환 참조로 인한 메모리 누수를 완벽히 차단했습니다.
- **안전한 객체 접근 보장**: 컴포넌트와 객체 간 상호작용 시 `weak_ptr::lock()`을 사용하여 메모리의 유효성을 먼저 검증한 후 로직을 수행하도록 변경되어, 삭제된 객체에 접근하여 발생하는 런타임 크래시 위험을 원천 제거했습니다.

### 2.2. 지연 삭제(Delayed Deletion) 로직 제거 및 삭제 파이프라인 단일화
- **`is_dead_` 상태 플래그 제거**: 기존 `Object` 내부에 존재하며 삭제 대기 상태를 나타내던 `is_dead_` 멤버 변수를 완전히 제거했습니다.
- **즉각적이고 안전한 삭제 구현**: 씬(Scene)의 `DeleteObject` 함수를 통한 단일 삭제 파이프라인을 구축했습니다.
- **참조 무효화(Invalidation) 체계**: `Scene`의 메인 `object_list_`만이 `shared_ptr`로 객체의 생명주기를 독점 관리합니다. 그 외 충돌 처리(Collider), 렌더링, 섹터(Sector) 리스트 등 모든 보조 컨테이너는 `weak_ptr`를 사용하도록 수정되었습니다. 이로 인해 메인 리스트에서 객체가 삭제됨과 동시에 모든 참조가 자연스럽고 안전하게 무효화됩니다.

### 2.3. 에셋 및 리소스 관리 구조의 현대화
- `ModelInfo`, `AnimatorComponent`, `ParticleSystem` 등 다양한 에셋 및 리소스 생성과 할당 시 원시 포인터 대신 `std::make_shared` 및 `std::make_unique`를 사용하도록 개편하여 예외 안전성(Exception Safety)을 확보했습니다.
- 특히 `ModelInfo` 클래스 내 계층 구조(`hierarchy_root_`) 및 뼈대(`bone_frames_`) 관련 멤버들을 `shared_ptr`와 `weak_ptr`의 역할에 맞춰 명확히 분리 설계하여, 복잡한 모델 로딩 트리의 안전성을 대폭 향상시켰습니다.

## 3. 세부 리팩토링 적용 내역 요약
- **Core System**: `Object`, `Component`, `Scene`, `Sector`, `BaseScene`, `GameFramework` (전반적인 생명주기 제어 및 객체 생성 방식 변경)
- **Animation System**: `AnimatorComponent` 및 `AnimationState` 계층 하위 클래스 전체 (소유권 체인 확립)
- **Entities & Controllers**: `PlayerComponent`, `MonsterComponent`, `ChestComponent`, `FPSControllerComponent`, `MovementComponent`, `AIComponent` (상호 참조 멤버 `weak_ptr` 전환)
- **Weapons & Effects**: `GunComponent`, `SpawnerComponent`, `ParticleSystem` (동적 생성 객체에 대한 안전한 소유권 분리)
- **UI & View**: `UiComponent` 기반 클래스, `CameraComponent` 등

## 4. 리팩토링의 기대 효과 및 결론
1. **안정성 및 신뢰성 극대화**: 메모리 릭(Memory Leak), 댕글링 포인터(Dangling Pointer), 더블 프리(Double Free) 등 기존 C++ 환경에서 빈번히 발생하는 메모리 관리 버그가 구조적으로 차단되었습니다.
2. **코드 가독성 및 유지보수성 향상**: 포인터의 소유권(Ownership)이 타입 자체(`shared_ptr`, `unique_ptr`, `weak_ptr`)로 명확하게 표현되므로, 타 개발자가 코드를 읽고 수정할 때 발생할 수 있는 잠재적 부작용이 최소화됩니다.
3. **일관성 있는 아키텍처 확립**: 지연 삭제 로직의 제거와 단일 삭제 파이프라인의 확립으로 인해, 객체의 생성부터 소멸까지의 라이프사이클이 매우 직관적이고 단순명료해졌습니다.

결론적으로 이번 리팩토링을 통해 SandyHeroes 프로젝트는 모던 C++(C++11 이상)의 권장 표준에 부합하는, 견고하고 확장 가능하며 유지보수가 용이한 최적의 시스템 기반을 성공적으로 갖추게 되었습니다.