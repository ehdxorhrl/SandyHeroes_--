# 포트폴리오 답변 내역

### 최저한의 GPU 상태 변경을 위한 PSO, Material, Mesh순의 배치처리

**[성능 측정 방법 제안]**

DirectX 12에서 렌더링 성능 최적화를 위한 배치처리의 효과를 어필하기 위해, 다음과 같은 정량적인 성능 측정 방법들을 제안합니다. PPT에 객관적인 수치와 함께 표현하기 매우 좋은 방법들입니다.

**1. API 호출 횟수 추적 (가장 직관적인 어필)**
- **측정 방법:** 엔진 내부(예: `GameFramework` 또는 렌더러 로직)에 프레임당 발생하는 주요 상태 변경 API 호출 횟수를 기록하는 카운터를 임시로 추가합니다.
  - 추적 대상 API: `SetPipelineState`, `SetGraphicsRootSignature`, `SetGraphicsRootDescriptorTable`, `IASetVertexBuffers` 등
- **어필 포인트:** "배치 처리 적용 전 프레임당 OOO회의 무작위 상태 변경이 발생했으나, PSO/Material 기준 정렬 후 상태 변경 횟수를 OO회로 약 X% 대폭 감소시켜 드로콜 오버헤드를 줄였습니다."
- **외부 툴 활용:** **PIX for Windows**로 프레임 캡처(GPU Capture) 후 Event List에서 해당 API들의 전체 호출 횟수를 캡처하여 증빙 자료로 사용.

**2. GPU Timestamp Query를 통한 렌더링 패스 시간 측정**
- **측정 방법:** DirectX 12의 `D3D12_QUERY_TYPE_TIMESTAMP`를 활용하여 커맨드 리스트의 렌더링 시작 지점과 끝 지점에 쿼리를 삽입합니다. 이를 통해 순수 GPU 처리 시간(Execution Duration)을 측정합니다. CPU 쪽에서도 커맨드 리스트를 기록(Record)하는 데 걸리는 시간(`QueryPerformanceCounter` 등 활용)을 함께 측정합니다.
- **어필 포인트:** "GPU 상태 변경(Context Switch) 최소화로 인해 GPU 파이프라인의 빈 공간(Bubble)이 줄어들어, 메인 렌더링 패스의 GPU 실행 시간이 A ms에서 B ms로 단축되었습니다. 더불어 CPU 커맨드 기록 시간도 절감되었습니다."

**3. 대규모 오브젝트 스트레스 테스트 (FPS 및 병목 비교)**
- **측정 방법:** `TestScene`과 같은 빈 씬에 동일한 매테리얼과 메쉬를 공유하는 오브젝트 5,000~10,000개를 렌더링하는 환경을 구축합니다. 배치 처리 기능을 On/Off 하는 토글을 만들어 평균 FPS(Frame Per Second)를 비교합니다.
- **어필 포인트:** 극한의 드로콜 상황에서의 확연한 프레임 차이(예: "적용 전 25 FPS -> 적용 후 144 FPS 이상 방어")를 막대그래프로 제시하여, 설계의 우수성을 시각적으로 어필합니다.

**4. PIX 파이프라인 타임라인 시각화 (전문성 어필)**
- **측정 방법:** PIX의 'Timing Capture'를 활용하여 GPU 타임라인의 빈도 및 밀집도를 확인합니다.
- **어필 포인트:** 배치 처리 전 상태 변경으로 인해 듬성듬성했던 드로우 콜 타임라인과, 배치 처리 후 드로우 콜들이 빈틈없이 촘촘하게 팩킹(Packed)되어 GPU Utilization(활용도)이 극대화된 타임라인 스크린샷을 대조하여 포트폴리오에 첨부합니다. 그래픽스 이해도와 프로파일링 툴 사용 능력을 동시에 어필할 수 있습니다.

---

### **[정렬 배치 처리 및 MeshInstanceInfo 도입의 스토리텔링 해결책]**

포트폴리오를 작성할 때 실제 개발 순서(`unordered_map` 사용 후 문제점 인지 -> `std::sort` 및 `MeshInstanceInfo` 도입)가 10페이지와 14페이지 구조와 달라서 겪는 문제는, 단순히 최종 코드를 쪼개 넣으려 하지 않고 **"초기 구조의 한계 분석 및 리팩토링을 통한 성능 극대화 과정"**으로 스토리라인을 구성하면 가장 강력한 무기가 됩니다.

채용 담당자(특히 그래픽스/엔진 분야)는 처음부터 버그 없이 완벽한 설계를 한 지원자보다, **"스스로 병목을 인지하고 이를 컴퓨터 구조론적 관점(캐시 미스, 동적 메모리 할당)에서 명확하게 분석하여 고도화한 경험"**이 있는 지원자를 훨씬 더 높게 평가합니다.

#### **1. 포트폴리오 슬라이드별 스토리라인 재구성 안**

*   **10페이지 (초기 배치 처리 단계 - 무작위 렌더링의 한계 극복)**
    *   **핵심 어필:** "동일 PSO 및 Material을 사용하는 오브젝트를 분류하기 위해 최초에는 직관적인 `unordered_map` 기반의 배치 처리를 도입했습니다."
    *   **설명:** 
        *   씬에 있는 모든 오브젝트를 개별적으로 그리지 않고, PSO와 Material을 기준으로 그룹화하여 GPU의 파이프라인 컨텍스트 스위칭 비용을 최소화하였습니다.
        *   이 시점에는 구현의 편의성과 해시 탐색의 속도를 고려해 `std::unordered_map<Mesh*, std::vector<std::shared_ptr<MeshComponent>>>`를 사용해 동일 메쉬를 분류했습니다.
    *   **10페이지 삽입 코드:** `unordered_map`을 활용한 직관적인 초기 분류 및 개별 드로우 콜 루프 코드 (아래 스니펫 제공).
    *   **다음 페이지로의 연결고리 (한계 인지):** 
        *   "그러나 이 구조는 대규모 오브젝트 렌더링 시 **세 가지 치명적인 병목**을 유발했습니다."
        1.  **매 프레임 동적 메모리 할당:** 맵에 요소를 삽입할 때 발생하는 동적 할당 및 해시 노드 생성 비용.
        2.  **메모리 파편화와 캐시 미스:** 노드 기반 컨테이너인 `unordered_map`은 데이터가 메모리에 불연속적으로 저장되어 CPU 캐시 효율이 급격히 저하됨.
        3.  **인스턴싱 연동의 한계:** 14페이지의 GPU 인스턴싱 기술(Instancing)을 적용하기 위해선 연속된 메모리 배열에서 오프셋과 카운트를 엄격하게 제어해야 하는데, `unordered_map` 구조로는 구현이 비효율적이고 복잡함.

*   **14페이지 (인스턴싱 도입 및 데이터 구조 고도화 단계 - 정렬 및 선형 데이터 구조화)**
    *   **핵심 어필:** "위의 병목을 해결하기 위해 `unordered_map`을 과감히 제거하고, **`std::sort` 기반 연속 메모리 정렬**과 **`MeshInstanceInfo` 구조체**를 도입하여 GPU 인스턴싱 최적화를 완성했습니다."
    *   **설명:**
        *   메모리가 연속적인 `std::vector`를 유지하고, 메쉬의 주소값(포인터)을 기준으로 직접 `std::sort`를 수행하여 데이터를 밀집(Contiguous) 배치했습니다.
        *   정렬된 벡터를 단 한 번 선형 순회하며 메쉬 변경 경계를 파악하고, 이를 다중 인스턴싱 드로우콜에 즉각 바인딩할 수 있는 최적화 구조체인 `MeshInstanceInfo`로 팩킹했습니다.
        *   그 결과, 인스턴스 버퍼로의 데이터 복사가 극도로 가속화되었고, CPU 캐시 적중률(Cache Hit Rate)이 대폭 상승하였으며, GPU Instancing 드로우콜 하나로 수천 개의 오브젝트를 단번에 렌더링하는 데 성공했습니다.
    *   **14페이지 삽입 코드:** 현재 프로젝트에 구현된 완성형 `UpdateObjectFrameResource` 및 `Render` 내의 `RenderInstancing` 코드 (아래 스니펫 제공).

---

#### **2. 페이지별 삽입용 코드 템플릿**

##### **[10페이지용: 초기 `unordered_map` 기반 배치 분류 코드]**
```cpp
// [초기 구현] unordered_map을 이용한 동일 Mesh 기준 배치 처리 (10페이지 본문용)
void Material::Render(ID3D12GraphicsCommandList* command_list, FrameResource* curr_frame_resource)
{
    // 1. PSO/Material 내에서 Mesh 별로 오브젝트를 분류하기 위한 임시 해시 맵 생성
    // (매 프레임마다 동적 노드 생성 및 해시 연산 오버헤드 발생)
    std::unordered_map<Mesh*, std::vector<std::shared_ptr<MeshComponent>>> mesh_map;

    for (const auto& weak_component : mesh_components_)
    {
        if (auto component = weak_component.lock())
        {
            mesh_map[component->GetMesh()].push_back(component);
        }
    }

    // 2. 분류된 그룹별로 개별 드로우 콜 수행 (인스턴싱 미적용)
    for (const auto& [mesh, components] : mesh_map)
    {
        for (const auto& component : components)
        {
            // 메쉬 단위로 모아서 그리지만, 여전히 오브젝트당 개별 Draw API 호출
            component->Render(this, command_list, curr_frame_resource);
        }
    }
    
    mesh_components_.clear();
}
```

##### **[14페이지용: 최적화 완료된 `std::sort` + `MeshInstanceInfo` 인스턴싱 코드]**
```cpp
// [최적화 완료] std::sort 연속 정렬 및 MeshInstanceInfo 구조체 기반의 GPU 인스턴싱 (14페이지 본문용)
void Material::UpdateObjectFrameResource(FrameResource* curr_frame_resource)
{
    // 1. 유효하지 않은 weak_ptr 정리
    std::erase_if(mesh_components_, [](const std::weak_ptr<MeshComponent>& weak_component) {
        return weak_component.expired();
    });

    // 2. 메쉬 포인터 주소 기준 std::sort 정렬 (메모리 연속성 및 캐시 효율 극대화)
    std::sort(mesh_components_.begin(), mesh_components_.end(),
        [](const std::weak_ptr<MeshComponent>& a, const std::weak_ptr<MeshComponent>& b) {
            return a.lock()->GetMesh() < b.lock()->GetMesh();
        });

    // 3. 단일 선형 순회로 MeshInstanceInfo 구조체 배열에 인스턴스 정보 그룹화
    mesh_instance_infos_.reserve(mesh_components_.size());
    Mesh* prev_mesh = nullptr;
    for (const auto& weak_component : mesh_components_)
    {
        auto mesh_component = weak_component.lock();
        const auto& mesh = mesh_component->GetMesh();
        if (prev_mesh != mesh)
        {
            mesh_instance_infos_.emplace_back(mesh);
            prev_mesh = mesh;
        }
        mesh_instance_infos_.back().components.push_back(mesh_component);
    }

    // 4. 인스턴싱용 가시성 검사 및 뷰포트/그림자 맵 오프셋 계산 ...
}

void Material::Render(ID3D12GraphicsCommandList* command_list, 
    FrameResource* curr_frame_resource, DescriptorManager* descriptor_manager, std::shared_ptr<CameraComponent> camera, bool bShadow)
{
    // PSO 및 Material 상수 버퍼 바인딩 ...

    for (const auto& mesh_instance_info : mesh_instance_infos_)
    {
        auto mesh_type = mesh_instance_info.mesh->mesh_type();
        if (mesh_type == MeshType::kSkinnedMesh)
        {
            // 애니메이션이 필요한 스킨드 메쉬는 개별 드로우콜 처리
            for (const auto& component : mesh_instance_info.components)
            {
                component->Render(this, command_list, curr_frame_resource);
            }
        }
        else
        {
            // 5. 정적 메쉬는 모아진 인스턴스 개수만큼 단 한 번의 인스턴싱 드로우콜로 최적화 렌더링
            auto sub_mesh_index = mesh_instance_info.components.front()->GetMaterialIndex(this);
            mesh_instance_info.mesh->RenderInstancing(command_list, sub_mesh_index, curr_frame_resource, mesh_instance_info.main_visible_count);
        }
    }

    mesh_components_.clear();
    mesh_instance_infos_.clear();
}
```